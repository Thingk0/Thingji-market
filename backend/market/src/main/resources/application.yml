
# 프로필 설정
spring:
  profiles:
    active: dev

  jpa:
    open-in-view: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

# 로그 레벨 설정
logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace\